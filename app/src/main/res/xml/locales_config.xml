<?xml version="1.0" encoding="utf-8"?>
<!-- Locales DotCal ships. Keeps Android's own per-app language screen in sync with the
     in-app picker (Settings > Appearance > Language). Note: Indonesian's resource folder
     is the legacy `values-in` while the BCP-47 tag here is `id`. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
    <locale android:name="es" />
    <locale android:name="pt" />
    <locale android:name="id" />
    <locale android:name="de" />
    <locale android:name="fr" />
    <locale android:name="ru" />
    <locale android:name="tr" />
    <locale android:name="ar" />
</locale-config>
