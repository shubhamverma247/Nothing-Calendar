# DotCal Handoff

Updated: 2026-07-09

Source of truth for DotCal (`com.dotfield.dotcal`). Full history/archive lives in `Docs/HANDOFF.original.md`. Keep this file short: current rules, invariants, active status, latest work, next tests only.

## Hard Rules

- Use `$android-development` for Android work.
- Workdir: `D:\Caveman\caveman\Nothing-Calendar`.
- Preserve existing app behavior/UI unless user explicitly changes it.
- Do not change package/application id: `com.dotfield.dotcal`.
- Do not change deep link scheme: `dotcal://`.
- Do not change Room DB filename: `dotcal.db`.
- Do not change Room schema: exactly 5 tables, no new columns unless user explicitly approves:
  - `calendar_accounts`
  - `calendar_events`
  - `event_reminders`
  - `sync_metadata`
  - `deleted_event_log`
- Do not run phone/manual UI QA unless user explicitly asks.
- After app-code changes run:

```powershell
.\gradlew.bat --no-daemon --console=plain :app:assembleDebug
```

- Keep `Docs/HANDOFF.md` updated after completed steps.
- Notification action labels stay Title Case: `View`, `View Task`, `Snooze 10 Min`.
- Destructive actions need confirmation. Full-screen surfaces use existing right-slide transition.
- Project has NO Hilt and NO Compose Nav graph. Keep manual DI + boolean full-screen overlays (`showPaywall`, `showDateCalculator`).

## Schema / Storage

Room:
- `calendar_accounts`: `id`, `accountName`, `displayName`, `accountType`, `color`, `isVisible`, `isPrimary`, `sortOrder`.
- `calendar_events`: `id`, `accountId`, `title`, `description`, `location`, `startTimeMs`, `endTimeMs`, `timeZone`, `isAllDay`, `colorHex`, `rrule`, `exceptionDates`, `source`, `googleEventId`, `googleCalendarId`, `syncVersion`, `isTask`, `isCompleted`, `completedAtMs`, `imageUris`, `voiceNotePath`, `createdAtMs`, `updatedAtMs`.
- `event_reminders`: `id`, `eventId`, `minutesBefore`, `triggerAtMs`, `alarmRequestCode`, `isDelivered`.

DB gotchas:
- `calendar_events.accountId` FK cascades on `calendar_accounts.id`; do not use Room `REPLACE` for accounts if it can delete child events.
- `event_reminders.eventId` FK cascades on `calendar_events.id`.
- `event_reminders.alarmRequestCode` unique.
- Repository validates local event end >= start.

DataStore name: `calendar_preferences`.
Keys: `KEY_DEFAULT_VIEW`, `KEY_WEEK_START`, `KEY_DEFAULT_REMINDER`, `KEY_DEFAULT_ALL_DAY_REMINDER_TIME`, `KEY_SYNC_ENABLED`, `KEY_SYNC_INTERVAL_MINS`, `KEY_BIRTHDAY_ENABLED`, `KEY_ONBOARDING_DONE`, `KEY_LAST_SYNC_MS`, `KEY_SHOW_DECLINED`, `KEY_24_HOUR_FORMAT`, `KEY_THEME_MODE`, `KEY_ACCENT_COLOR`, `KEY_APP_FONT`, `KEY_LAST_SELECTED_DATE`, `KEY_IS_PRO`, `KEY_WIDGET_TRANSPARENT`, `KEY_WIDGET_DOT_TEXTURE`, `KEY_WIDGET_ACCOUNT_ID`, `KEY_APP_LOCK_ENABLED`, `KEY_APP_LOCK_PIN_SALT`, `KEY_APP_LOCK_PIN_HASH`, `KEY_PRIVATE_VAULT_EVENT_IDS`.

## Current App State

Product: premium black/white/red calendar app. App label: `DotCal`.

Main nav:
- Bottom nav: `Calendar`, `Tasks`, `Settings`.
- Calendar views: `Year`, `Month`, `Week`, `Day`, `Agenda`. Hidden/legacy `ThreeDay` may exist internally; do not expose.
- Settings, Event Detail, Task Detail, Add/Edit Event use full-screen right-slide surfaces.

Theme/UI:
- System sans-serif (`mono = FontFamily.SansSerif`), not old mono typography.
- Light palette: Screen `#F7F7F7`, Dialog `#FFFFFF`, Cancel `#EFEFEF`, Accent `#FF3B30`, Primary `#101010`, Secondary `#6B6B6B`, Disabled `#BDBDBD`.
- Dark palette: Screen `#000000`, Dialog `#1E1E1E`, Cancel `#121212`, Accent `#FF3B30`, Primary `#FFFFFF`, Secondary `#B3B3B3`, Disabled `#6E6E6E`.
- Accent choices: Red `#FF3B30`, Blue `#0A84FF`, Green `#30D158`, Purple `#BF5AF2`, Amber `#FF9F0A`.
- Calendar/list/settings/bottom-nav rows suppress default ripple where it clashes with DotCal style; keep visible state/haptics.
- Destructive actions: centered red text, not filled buttons.

Implemented features:
- Events: Month/Week/Day/Agenda/Year, recurrence daily/weekly/monthly, recurring instance edit/delete (`This event` vs `Whole series`), Event Detail, deep link `dotcal://event/{eventId}`.
- Media: images via existing `imageUris`, Android Photo Picker, max 5; voice notes via existing `voiceNotePath`, internal `voice_notes/{eventId}.m4a`, max 5 min.
- Reminders: `ReminderScheduler`, `ReminderReceiver`, `BootReceiver`, channel `dotcal_reminders`, event/task notification deep links, snooze 10 min, boot reschedule.
- Sync: CalendarProvider-only. No REST/OAuth/cloud/network sync. Uses existing `sync_metadata` + `deleted_event_log`. Local mode works without calendar permission.
- Tasks: stored in `calendar_events` (`isTask = 1`), filters `All/Today/Upcoming/Completed`, bottom-sheet editor, full-screen detail, task reminders reuse `event_reminders`.
- Birthday calendar: contacts import into existing tables, `source = BIRTHDAY`, yearly all-day recurrence, read-only detail/edit hidden.
- Global Holidays: bundled 2025-2031 data only for IN/DE/GB/JP/IT/SA/US.
- Widgets: Glance 2x2/4x2/4x4. Small/Medium free. Large is Pro-gated. Widget previews must stay RemoteViews-safe (`LinearLayout`/`TextView`/safe views only; no `Space`, `TableLayout`, `TableRow`).
- Onboarding: 5 pages; permissions optional; deep links skip onboarding.
- In-app updates: Play Flexible update check on launch + Settings > About > Check for updates.
- Splash: Android 12+ splash background black in light and dark.

Skipped:
- Phase 1 Step 2 Print to PDF was explicitly skipped. Do not resurrect unless user asks.

## Pro / Billing

Status: complete in code. Billing product active in Play Console; Google-side Payments/Merchant review clear as of 2026-07-09 per user. Full purchase-flow verification is the next Play/Internal-testing step.

Important files:
- `app/src/main/java/com/dotfield/dotcal/data/billing/ProManager.kt`
- `app/src/main/java/com/dotfield/dotcal/prefs/CalendarPreferences.kt`
- `app/src/main/java/com/dotfield/dotcal/DotCalApplication.kt`
- `app/src/main/java/com/dotfield/dotcal/ui/DotCalApp.kt`
- `app/src/main/java/com/dotfield/dotcal/ui/DotCalViewModel.kt`
- `app/src/main/java/com/dotfield/dotcal/presentation/datecalculator/DateCalculatorViewModel.kt`
- `app/src/main/java/com/dotfield/dotcal/widget/DotCalWidgets.kt`
- `gradle/libs.versions.toml`
- `app/build.gradle.kts`

Billing facts:
- Dependency: Google Play Billing Library `billing-ktx` v7.1.1. Do not downgrade below v6.
- Permission: `com.android.vending.BILLING`.
- Product ID: `dotcal_pro`.
- Purchase option ID: `dotcal-pro-lifetime`.
- Type: one-time purchase / Buy.
- Live price: INR 149 (auto-converted elsewhere).
- License Testing: `pro-tester` list, response `RESPOND_NORMALLY`.
- Still confirm actual test Gmail is in `pro-tester` and logged in on test device.
- Entitlement persisted in `KEY_IS_PRO`; widgets refresh after purchase/restore.

Pro gates:
- Image Attachments.
- Voice Notes.
- Large Widget.
- Widget Pack Config (transparent background + dot texture controls for existing widgets).
- Date Calculator.
- Custom Accent Colors (extra preset palette + custom hex picker; 5 base accents stay free).
- Natural-language Quick Add (offline parser; sparkle icon in Calendar top bar).

Ungated to FREE (2026-07-06, data portability/safety — market norm; avoids "won't let me export my own data" reviews):
- Calendar Import / Export (local `.ics` via SAF; no cloud/network). Now FREE.
- Backup & Restore (full-fidelity offline JSON snapshot via SAF; non-destructive merge restore; no network). Now FREE.

Paywall:
- Route: `dotcal://paywall`.
- Opens from Settings > DotCal Pro, Pro-gated image/voice/date-calculator actions, Large widget locked state.
- Uses live `ProductDetails` price when available; fallback string currently INR 149.
- Restore Purchase lives in Paywall.

Known Pro fixes:
- 2026-07-02: Paywall purchase success auto-dismiss fixed. Root cause: success handler called `clearPurchaseResult()` before `delay(1500)`, causing `LaunchedEffect(purchaseResult)` to restart with `null` and cancel `onDismiss()`. Flow now shows `You're Pro!` for ~1.5s, closes Paywall, then clears purchase result. Version bumped to `versionCode 8`, `versionName 1.1.3`.
- 2026-07-02: Paywall crash fix changed app icon painter from adaptive `R.mipmap.ic_launcher` to bitmap `R.mipmap.ic_launcher_foreground`. Large locked widget Unlock radius changed `0dp -> 20dp`. Version bumped to `versionCode 7`, `versionName 1.1.2` so Play Internal Testing can accept fix over broken `1.1.1`.

## Pro Backlog (proposed, not built)

Offline/hardware/customization moat only. No network (honors no-cloud Hard Rule). No new columns (5-table lock intact). Reuses existing `rrule`, `deleted_event_log`, `imageUris`, `voiceNotePath`, DataStore. Ranked best-first.

1. Natural-language quick-add — parse "gym every mon 7am" -> event. Local parser. Top cited worth-it feature. **BUILT (branch `pro-features`, not yet on main) — see Latest Work.**
2. Widget pack + config — agenda-list, month-grid, event-countdown widgets + transparency/theme/calendar picker. #1 Android paid draw. **BUILT (branch `pro-features`, not yet on main) — see Latest Work.**
3. Glyph reminder flash + timer — rear Glyph pulses when event near, face-down. Nothing-only moat; native Glyph can't read local events.
4. App lock + private vault — PIN/device credential + hidden events. DataStore flag, no column. **BUILT (branch `pro-features`, not yet on main) — see Latest Work.**
5. Backup & restore (JSON via SAF) — whole-DB snapshot, offline. Pairs ICS import/export. **BUILT (branch `pro-features`, not yet on main) — see Latest Work.** Pro-gated. Backs up the 3 user-content tables only (`calendar_accounts` referenced by exported events + user-owned `calendar_events` + their `event_reminders`); `sync_metadata`/`deleted_event_log` deliberately excluded (transient sync bookkeeping; restoring stale Google tombstones could suppress re-synced events). Restore = non-destructive merge/upsert (never deletes live data). File-serializer only, no Room/schema change.
6. Offline OCR event capture — snap poster/card -> ML Kit on-device text -> prefill event. Cloud-free differentiator.
7. Advanced recurrence — custom RRULE (every N weeks, nth weekday, until/count). `rrule` column already exists, no schema change. **BUILT (branch `pro-features`, not yet on main) — see Latest Work.** Pro-gated (the custom builder only; None/Daily/Weekly/Monthly/Yearly presets stay FREE).
8. Recently-Deleted / restore — 30-day trash + undo. **BUILT (branch `pro-features`, not yet on main) — see Latest Work.** NOTE: `deleted_event_log` was NOT usable for this — it only stores sync tombstones (`googleEventId`, `deletedAtMs`), not event content, and local events aren't logged there. Implemented as a file-based snapshot store instead (no Room/schema change). FREE feature (data-safety; market norm — Google/Samsung/Apple keep trash free).

Deferred (needs internet + Play Data Safety + Privacy Policy + `INTERNET` perm, blocked by current no-network Hard Rule): ICS URL subscribe (webcal), weather strip, live holiday refresh. Keep FREE if ever built.

## Next Feature Picks (new, excluding existing handoff backlog)

Picked from competitor research on 2026-07-05. These are **after** already-decided handoff/backlog features are finished. Priority order: finish existing decided features first (Glyph branch if user asks, App Lock + Private Vault, Offline OCR Event Capture, Pro branch cleanup/commit/merge/testing), then move to these new picks. Keep offline-first, no network, no Room schema change unless user explicitly approves. Two Free, three Pro:

1. Global Search — **FREE**. Search events/tasks by title, location, description, calendar, and date range. Expected core feature; good trust/retention feature, not ideal as Pro-only. Implement with existing `calendar_events` queries or in-memory filtering from existing flows; no new table/column. **BUILT (branch `pro-features`) — see Latest Work.**
2. Event + Task Templates — **PRO**. Save reusable event/task presets (title, calendar, duration, reminder, recurrence, color, notes) for gym/classes/bills/client calls. Store templates as app-private JSON/DataStore, not Room. Strong paid reason; competitors like Business Calendar/Fantastical have template-style power flows.
3. Task Time Blocking + Focus Planner — **FREE basic**. Convert a task into a calendar block using existing `calendar_events` task/event model. Basic manual scheduling free; defer Pro auto-plan/smart fill unless user asks. Competes with Google Calendar task time blocking.
4. Calendar Sets / Focus Profiles — **PRO**. Work/Personal/Family/Widgets visibility presets, optionally including widget calendar filters. Store profile definitions in app-private JSON/DataStore; reuse existing account visibility/filter concepts, no schema change.
5. Advanced Reminder Profiles — **PRO**. Saved reminder presets and stronger notification behavior (repeat-until-dismissed/persistent reminder style/custom reminder stacks) using existing `event_reminders` where possible. Needs careful notification QA; no phone/manual QA unless user asks.
6. Shift Pattern Builder + Bulk Apply — **PRO**. Built from emergency-services shift-worker feedback: save reusable shift types, build rotating cycles like Day/Day/Night/Night/Off×4, generate months of one-off events, and long-press Month dates to bulk-stamp an event template. File-based JSON stores + generated-event records; no Room/schema change.

## Cross-App Feature Requests (to build, from DotFiles work 2026-07-08)

These two are built + user-tested in the sibling **DotFiles** app (`D:\Caveman\caveman\Nothing-FIle-Manage`, branch `feature/new-feature`, uncommitted) and the user wants them ported to DotCal. Mirror the DotFiles implementation below (already iterated on user feedback — this is the FINAL shape).

### 1. Font picker — BUILT (FREE)

Settings option to choose the app typeface. Final DotFiles implementation:

- **Fonts:** copied from the NFiles APK (`com.hearthborn.studios.nfiles`, `assets/flutter_assets/assets/fonts/`) into `res/font/`: `nothing-font-5x7.ttf` → `ndot.ttf`, `NType82Mono-Regular.otf` → `ntype82.otf`.
- **`AppFont` enum** (in `Models.kt`): `NDot("ndot","Ndot","Monospaced. Technical. Precise.")`, `NType("ntype","NType 82","Editorial. Redefined.")`, `System("system","System","Your device's own font.")` + `fromId()`. Default = `NDot`.
- **Persistence:** DotCal uses DataStore key `KEY_APP_FONT`; loaded at root and written from Settings.
- **Apply mechanism:** `val LocalHeadingFont = staticCompositionLocalOf<FontFamily> { FontFamily.Default }`. At app root, resolve the family (`rememberAppFontFamily(font)` → `FontFamily(Font(R.font.ndot))` / `ntype82` / `FontFamily.Default`) and wrap the content tree in `CompositionLocalProvider(LocalHeadingFont provides family) { ... }`.
- **Scope = TOP HEADINGS ONLY.** Only the big top-of-screen titles read `fontFamily = LocalHeadingFont.current` (in DotFiles: the tool-screen header title + the screen/dialog `DotHeader`). Do NOT apply to section labels, card titles, or body — user explicitly rejected app-wide application (dot-matrix is unreadable as body). Custom Canvas dot-glyph labels (if any) stay independent.
- **UI:** a single Settings "Font" `SettingsActionCard` (subtitle = current font label + tagline) that opens a `ModalBottomSheet` (`FontPickerSheet`) listing the 3 options, each previewed in its own typeface, selected = red border + check. NOT three inline cards (user rejected that).
- **LICENSING (user decision 2026-07-08): KEEP NType82 as-is** — user accepts the risk ("lets use NType82, if anything goes wrong i will revert it"), same font NFiles ships. Do NOT drop or swap it unless the user says so. Ndot is publicly released by Nothing.

### 2. Language picker — TO BUILD (scaffold + i18n effort)

- **`AppLanguage` enum** (`Models.kt`): `System("")` + INTERNATIONAL set (user asked for foreign, not Indian): `English(en)`, `Spanish(es)`, `French(fr)`, `German(de)`, `Portuguese(pt)`, `Italian(it)`, `Russian(ru)`, `Turkish(tr)`, `Chinese(zh)`, `Japanese(ja)`, `Korean(ko)`, `Arabic(ar)`. Each has `tag`, `label`, `native`. `fromTag()` helper.
- **Persistence + apply:** pref key `AppLanguageKey` → `UiState.appLanguage`; `setAppLanguage(lang, context)` writes pref, updates state, and on API 33+ sets `context.getSystemService(LocaleManager::class.java).applicationLocales = LocaleList.forLanguageTags(tag)` (empty list for System).
- **UI:** Settings "Language" `SettingsActionCard` → `ModalBottomSheet` (`LanguagePickerSheet`) listing entries by native name, selected = red border + check.
- **REMAINING REAL WORK = i18n string extraction.** The picker only sets the per-app locale; nothing visibly translates until hardcoded UI strings are moved into `res/values/strings.xml` + translated `values-<lang>/strings.xml`. DotCal has many hardcoded strings too — treat as a dedicated effort, not a one-build feature.

## Latest Work

Branch `pro-features` (WIP, not on main):
- 2026-07-09: **Font picker port from DotFiles shape built.** FREE setting under Settings > Additional. Added font resources `res/font/ndot.ttf` and `res/font/ntype82.otf`, `AppFont` model, `LocalHeadingFont`, `rememberAppFontFamily(...)`, and DataStore key `KEY_APP_FONT`. Settings shows a single Font row with current label/tagline and a `ModalBottomSheet` listing Ndot, NType 82, and System with per-font previews plus red selected border/check. Scope intentionally limited to top headings only (calendar top title, Settings large/compact headers, font sheet title); body/rows remain existing `mono`/system sans. No Room/schema/manifest/permission/dependency/version change. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 2m 49s), then debug APK installed on device (`Success`).
- Existing Widget Pack polish/config (Pro Backlog #2 partial). Kept current 3 Glance widgets/receivers intact: Small 2x2 now shows next-event countdown label (`2H 15M`/`NOW`) above title, Medium 4x2 now renders a compact agenda list (up to 3 items) instead of only one event detail, Large 4x4 remains month-grid dashboard but now surfaces up to 3 agenda rows. Added DataStore keys `KEY_WIDGET_TRANSPARENT` and `KEY_WIDGET_DOT_TEXTURE` (no Room/schema change). Widgets still follow app theme/accent; new Pro-gated Settings controls under Additional toggle transparent widget background and dotted texture, then enqueue `WidgetUpdateWorker`. `WidgetSurfaceBox` skips dot-tile background when texture is off or transparent is on. No new widget receivers/XML yet; existing picker entries preserved.
- Natural-language Quick Add (Pro Backlog #1). New `data/nlp/QuickAddParser.kt` — pure Kotlin, offline, no new dependency. `QuickAddParser.parse(text, now)` turns a line like `gym every mon 7am` into a `QuickAddResult` (title, date, endDate, startTime?, endTime?, isAllDay, rrule?) that maps 1:1 onto `EventEditorData`. Recognizes times (`7am`/`7:30pm`/`19:00`/`noon`/`midnight`), dates (`today`/`tonight`/`tomorrow`/weekday names/`next <weekday>`/`in N days|weeks|months`/`jul 4`/`4 july`/`M/D[/Y]`/`5th`), and recurrence limited to the app's real engine (bare `FREQ=DAILY|WEEKLY|MONTHLY|YEARLY`, no BYDAY/INTERVAL). `every <weekday>` = `FREQ=WEEKLY` with the start date pinned to that weekday (the anchor-based expander repeats it correctly). Everything matched is stripped from the title; unparseable input falls back to today at 9am timed. Nothing throws.
- UI in `ui/DotCalApp.kt`: new sparkle icon (`Icons.Default.AutoAwesome`) added to `CalendarActionBar` (via optional `onQuickAdd`, forwarded through `CalendarTabContainer`; Tasks-tab bar unaffected). Pro-gated — non-Pro tap opens Paywall. New full-screen right-slide overlay `showQuickAdd` hosting `QuickAddScreen`: auto-focused text field, live parsed preview (Title / When / Repeats rows), example chips, Continue button (+ IME Done). On Continue, `openQuickAddResult` seeds the standard `EventEditorScreen` via a new optional `prefill: QuickAddResult?` param (only applied when `event == null`) so the user reviews and taps Save through the normal `viewModel.saveEvent` -> `saveLocalEvent` path. No change to save/validation/repository/VM logic.
- `quickAddPrefill` state cleared on every normal add/edit open and on editor dismiss/save so a prefill never leaks into a plain "+" add. `showQuickAdd` added to `BackHandler` + `closeTopSurface`.
- Paywall `PRO_FEATURES` gained "Quick Add". No Room/schema/DataStore/manifest change. No version bump yet.
- Verified: `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 56s), real `compileDebugKotlin`. No phone/manual QA.

Branch `profeature` (WIP, not on main):
- ICS import/export Pro feature (local files only, no network — honors sync rule). New package `data/ics`: `IcsExporter.kt` (VEVENT + VTODO, RFC5545, line-folding, UID = event id, RRULE/EXDATE round-trip) and `IcsParser.kt` (unfold, VEVENT/VTODO subset, DTSTART/DTEND/DUE, TZID + all-day, EXDATE, STATUS; RRULE normalized to app's FREQ subset; unknown props ignored, no-crash). Pure Kotlin, no new dependency.
- `CalendarDao.getAllUserEventsForExport()` selects master rows excluding BIRTHDAY/HOLIDAY/GOOGLE. `DotCalRepository.exportIcs()/importIcs()/countExportableEvents()` + `IcsImportResult`. Import upserts by UID (match existing local id -> update, else insert), preserves images/voice/color/reminders on existing rows, validates timed span, reuses `LOCAL` source. No schema/column change.
- `DotCalViewModel.exportIcs{}` / `importIcs{}`. UI: SAF `CreateDocument("text/calendar")` + `OpenDocument()` launchers in `DotCalApp.kt`; new Settings "Data" section rows `Export Calendar` / `Import Calendar` via `SettingsImportExportRow` (Pro-gated -> Paywall for non-Pro, lock icon). Toast summaries.
- ICS reminders now round-trip via `VALARM` (`ACTION:DISPLAY`, relative `TRIGGER:-PT{minutes}M`). Import maps supported display alarms back to `event_reminders`, replaces existing reminders only when alarms are present, and schedules future reminders. Unsupported alarm types and positive after-start triggers are ignored.
- Paywall `PRO_FEATURES` gained "Import / Export".
- Verified: `:app:assembleDebug` passed (2m 27s), real `compileDebugKotlin`. No phone/manual QA.

- Custom accent + theme pack Pro feature. `AccentColor` in `ui/DotCalApp.kt` refactored from plain enum to a `sealed interface`: `Preset` enum (5 free + 8 Pro presets) and `Custom(hex)`. Storage in `KEY_ACCENT_COLOR` is backward compatible: preset enum name OR `#RRGGBB`. `fromStorage`/`normalizeHex` handle both; `storageValue` used everywhere `.name` was. `onColor` now auto-picks legible text via `luminanceApprox`.
- Theme settings screen: free swatches (unchanged), new "More Colors" Pro preset row (locked -> Paywall for non-Pro), new "Custom Color" row opening `CustomAccentPickerDialog` (hue/sat/brightness sliders via `detectTapGestures` + `detectHorizontalDragGestures`, live preview, hex text field). Non-Pro taps route to Paywall through existing `onDotCalPro`.
- Widget parser `widgetAccentColor` in `widget/DotCalGlanceTheme.kt` extended for new presets + `#hex`, red fallback.
- Paywall `PRO_FEATURES` gained "Custom Accent Colors".
- No Room/schema/DataStore-key changes. No version bump yet.
- New composables: `AccentColorSwatches` (now takes accent list + locked flag), `CustomAccentRow`, `CustomAccentPickerDialog`, `HueSlider`, `ValueSlider`, `SliderThumb`, `CalcSectionLabelSafe`. New imports: `detectTapGestures`, `ExperimentalLayoutApi`, `FlowRow`, `Lock` icon, `onSizeChanged`.
- Verified: `--rerun-tasks :app:compileDebugKotlin` passed (2m 28s), `:app:assembleDebug` passed (1m 15s). No phone/manual QA.

Branch `glyph-toy` (WIP, not on main):
- Nothing Glyph Matrix Toy: live countdown to next DotCal item on the rear Glyph Matrix (Phone 3 / 4a Pro). Differentiator generic calendar apps can't offer (native Glyph Progress reads only Google Calendar; DotCal is local-only so its events never reach it otherwise).
- SDK: local AAR `app/libs/glyph-matrix-sdk-2.0.aar` (Nothing `GlyphMatrix-Developer-Kit`, package `com.nothing.ketchum` / `com.nothing.thirdparty`). Wired via `implementation(files(...))` in `app/build.gradle.kts`. No Maven dep.
- SDK ships `minSdk 33`, app is `30`. Resolved with `<uses-sdk tools:overrideLibrary="com.nothing.thirdparty"/>` in manifest — NOT a minSdk bump. Safe because toy is bound only by Nothing OS toy manager (Android 14+); classes never load on other devices.
- New: `glyph/DotCalGlyphToyService.kt` (Android `Service`, action `com.nothing.glyph.TOY`, exported, app-level `NothingKey`, resource-backed meta-data name/image/summary/longpress). Device guard via `Common.is23112()/is25111p()`; all SDK calls `runCatching` silent-fail; inert on non-Nothing hardware. Render loop every 30s draws compact countdown text ("3d"/"5h"/"12m"/"now") via `GlyphMatrixObject`+`GlyphMatrixFrame`. Long-press `EVENT_CHANGE` cycles upcoming (Pro); `EVENT_AOD` re-renders.
- New: `DotCalRepository.getNextUpcomingList(includeTasks, nowMs, limit)` — one-shot snapshot reusing existing `expandRecurringEvents`/`observeUpcomingAgendaEvents`/`observeUpcomingTasks`. No schema/column/DataStore change.
- Freemium split (gated in code, not manifest): free = next EVENT countdown only; Pro (`repository.readIsPro()`) = tasks folded in + long-press cycle through next 5. Toy itself free = discovery hook for Nothing community. No new Paywall `PRO_FEATURES` entry.
- New permission `com.nothing.ketchum.permission.ENABLE` (Nothing custom; not a Google restricted permission; absent = inert on other devices). No Data Safety / Privacy Policy change (local-only, no network).
- Verified: `:app:assembleDebug` passed (4m 09s), real `compileDebugKotlin`. No phone/manual QA. Real Glyph render UNTESTED — needs a Nothing Phone 3 to confirm on-device behavior.

Latest local work:
- `versionCode = 9`, `versionName = "1.1.3"`.
- Paywall purchase success auto-dismiss fixed.
- Paywall adaptive-icon crash fixed.
- Large widget locked Unlock button rounded.
- Settings Pro feature tags fixed: Date Calculator and Import/Export now show `Pro feature` only for non-Pro users.
- ICS `VALARM` export/import added for event and task reminders.
- `Docs/HANDOFF.md` flattened/shortened.
- Date Calculator UI polish (`DateCalculatorScreen` in `ui/DotCalApp.kt`, no VM/logic change): both tabs now use uppercase section labels + bordered field groups. Days Between result now big accent hero number. Add/Subtract: cryptic square `+/−` toggle replaced by an Add/Subtract segmented control; day count is a `[−] [n] [+]` stepper (still typeable); result shows direction caption + big date. New private composables: `CalcSectionLabel`, `CalcFieldGroup`, `CalcResultHero`, `CalcDaysStepper`, `CalcStepperButton`. Added imports: `BasicTextField`, `SolidColor`.

Latest verification:
- 2026-07-02: Paywall purchase success auto-dismiss fix: `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 40s).
- 2026-07-02: `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed after version bump (2m 35s).
- 2026-07-02: Date Calculator UI polish: `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 14s).
- 2026-07-03: Settings Pro feature tag visibility fix: `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 35s).
- 2026-07-03: ICS `VALARM` reminder round-trip: `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (1m 15s).
- 2026-07-04: Glyph Toy discovery manifest fix (`NothingKey`, resource-backed toy name/summary, thumbnail): `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 51s).
- 2026-07-04: Verified merged manifest matches official Nothing GlyphMatrix-Example structure exactly (manifest-level `com.nothing.ketchum.permission.ENABLE`, app-level `NothingKey=test`, exported service + `com.nothing.glyph.TOY` filter, resource-backed name/image/summary + `longpress=1`, merged minSdk 30 preserved). Swapped vector `glyph_toy_thumbnail.xml` for a raster PNG (144x144) to match official (toy manager may load image via BitmapFactory). `:app:assembleDebug` passed (42s), PNG confirmed packaged.
- 2026-07-04: ROOT CAUSE of "DotCal not in Glyph Toys list" found via on-device adb (Phone 3 model A024/Metroid, USB debugging). `adb shell cmd package query-services -a com.nothing.glyph.TOY` DID list `com.dotfield.dotcal.glyph.DotCalGlyphToyService` — so package-manager discovery, meta-data format (all resource-backed), non-empty `glyph_toy_name`="DotCal"/summary, and the PNG thumbnail were all correct and the app was NOT in stopped state. Pulled and `aapt2 dump`ed 3 shipping third-party toys (com.frank.magic8ball, com.lisra.matrixleveler, com.nothinglondon.compass). ALL THREE declare their toy service as a **foreground service**: `android:foregroundServiceType` (magic8ball `0x100`; matrixleveler + compass `0x40000000` = `specialUse`) plus `<uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>` and a nested `<property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" .../>`. DotCal's toy service had NO `foregroundServiceType`. Also added `FOREGROUND_SERVICE_SPECIAL_USE` permission, `android:foregroundServiceType="specialUse"` on `DotCalGlyphToyService`, and the `PROPERTY_SPECIAL_USE_FGS_SUBTYPE` property to match all shipping third-party toys (`aapt2` confirms `foregroundServiceType=0x40000000` in the APK). These matched the app to the working reference but did NOT by themselves make it appear.
- 2026-07-04: **RESOLVED — DotCal now appears in the Glyph Toys picker on Phone 3 (confirmed by user screenshot: "DotCal — Shows a live countdown to your next DotCal event. (Community developed)").** Actual root cause: `NothingKey` is a **test key** (`android:value="test"`), and the Nothing Glyph Interface hides test-key toys from the picker unless developer debug mode is enabled. Fix was a device-side setting, not code: `adb shell settings put global nt_glyph_interface_debug_enable 1` (flag was `null`/off before). This opens a ~48h window during which test-key toys are listed. IMPORTANT PRODUCTION LIMITATION: before Play release the developer must apply to Nothing for a real production Glyph API key and swap it in for `NothingKey`; otherwise end users (without debug mode) will never see the toy. Next after user confirms on-device countdown render: implement remaining spec items (buildConfig/manifest-placeholder NothingKey + TODO, Settings Pro row deep-linking to the Glyph picker, Paywall `PRO_FEATURES` "Glyph Toy — Next Event Countdown", `GlyphSupport.isSupported()` util).
- 2026-07-04: Glyph Toy visual fix after on-device report: picker thumbnail changed from black/red DotCal art to a transparent, gray dot-matrix plate with a white calendar icon to match third-party Glyph Toys style. Matrix render changed from SDK text object at `setPosition(0, 0)` (showed only top-left white dots on hardware) to direct centered 25x25 brightness-frame rendering. Follow-up user screenshot showed visible four-side frame and oversized countdown, so the runtime frame was removed and countdown text is now fixed at compact 1x scale. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (1m 20s). On-device render confirmation still needed after installing this build.
- 2026-07-04: Glyph render polish after on-device photo (pure-black background, text looked off-center). Two `DotCalGlyphToyService.kt` changes: (1) countdown bounding-box centering uses `(size - width + 1) / 2` int rounding (an earlier float `.toInt()` attempt was a no-op — same result as int divide — so reverted). (2) added `drawBackgroundGrid()` faint dot-matrix "plate" so the toy is never pure black: dim dots (`BG_DOT_BRIGHTNESS=400`) every `BG_DOT_SPACING=3` px, masked to the ROUND matrix (skip dots outside `radius = size/2` — corner LEDs don't exist on the circular hardware and would look clipped). `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (1m 11s). On-device confirmation of dot brightness/centering still needed.
- 2026-07-04: Added Glyph progress ring. `drawProgressRing()` lights an outer-edge arc (`RING_BRIGHTNESS=1400`, between bg dots 400 and text 4095), clockwise from top via `atan2(dx,-dy)`, on a band at `radius = size/2 - RING_INSET(1.5)`, thickness `RING_BAND(0.7)`. Fill = `progressFraction()` = time-left / 24h window, clamped 0..1 (full when event far, drains as it nears, empty at/after start). `renderText`/`buildCountdownFrame` now take a `fraction` arg (`-1f` = no ring, used for the empty `--` state). `:app:assembleDebug` passed (1m 11s). On-device confirmation of ring brightness/sweep still needed.
- 2026-07-04: Glyph background now a FULL dim plate after on-device photo (user: other toys fill the whole round matrix; DotCal's plate looked sparse/center-only). `drawBackgroundGrid` changed from sparse dots (`BG_DOT_SPACING=3`, brightness 400) to every-LED fill (`BG_DOT_SPACING=1`, brightness `180`) inside the circular mask — a soft glowing disc filling the whole matrix, with ring (1400) and text (4095) still popping on top. `:app:assembleDebug` passed (1m 14s).
- 2026-07-04: REVERTED the hardware background plate AND the progress ring — on-device the dim gray wash "looked bad" and the user wants the rear render to be JUST the clean countdown. `buildCountdownFrame(text)` now only calls `drawCenteredText`; removed `drawBackgroundGrid`, `drawProgressRing`, `progressFraction`, the `fraction` plumbing on `renderText`, the `RING_*`/`RING_WINDOW_MS` consts, and the `kotlin.math.{PI,atan2,sqrt}` imports. Hardware render is now only countdown text (4095) on black.
- 2026-07-04: Natural-language Quick Add (Pro Backlog #1): new `data/nlp/QuickAddParser.kt` + `QuickAddScreen` overlay + editor `prefill` param + action-bar sparkle icon. Past-time guard added: explicit time with no explicit date (example `meet neha at 5pm`) rolls to tomorrow if that time already passed today. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 44s), real `compileDebugKotlin`. No phone/manual QA.
- 2026-07-04: Existing Widget Pack polish/config (Pro Backlog #2 partial): Small = countdown + next event, Medium = compact agenda list, Large = month dashboard with 3 agenda rows. Added Pro-gated widget config toggles for transparent background and dot texture; DataStore-only, no schema change. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (3m 17s), real `compileDebugKotlin`. No phone/manual QA.
- 2026-07-04: Widget polish follow-up from phone QA: Large widget vertical spacing tightened so third agenda row/location has room (top padding/gaps reduced, month cells slightly shorter, agenda row text/padding reduced). Widget config refresh hardened: Settings toggles now call immediate Glance `WidgetUpdateWorker.updateNow(context)` after DataStore write instead of only enqueueing WorkManager, so dot texture off/on and transparent should refresh instantly. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 40s). Install attempt failed because `adb devices` returned no connected devices.
- 2026-07-04: Picker thumbnail rebuilt to match other Glyph Toys (gray dot-matrix plate + white icon). New generator `scripts/gen_glyph_thumbnail.js` (pure Node, hand-rolled PNG encoder — no Python/PIL on this box) writes `app/src/main/res/drawable/glyph_toy_thumbnail.png` (144x144 RGBA, transparent bg so no black card): a round 25-dot plate of dark gray dots (`GRAY=45`) filling the circular mask, with a white (`255`) calendar icon on top (hangers + box border + solid header band + 3x2 date dots = 3 top, 3 bottom). Re-run with `node scripts/gen_glyph_thumbnail.js`. `:app:assembleDebug` passed (1m 7s), new PNG packaged. On-device confirmation of the clean hardware render + new picker thumbnail still needed.
- 2026-07-04: Widget Settings toggle refresh fix after phone report: `WidgetUpdateWorker.updateNow()` now updates all three Glance widgets and also sends targeted `ACTION_APPWIDGET_UPDATE` broadcasts with active widget IDs to Small/Medium/Large receivers. This forces launcher widget hosts to redraw when only background resources/prefs change (transparent/dot texture). No Room/schema change.
- 2026-07-04: Widget Settings toggle root cause fixed. Device check showed `calendar_preferences.preferences_pb` had `widget_transparent=false` and `widget_dot_texture=false`, but Glance `appWidgetLayout-*` files did not update after the toggle. Root cause: widget palette/settings were read before `provideContent`, so active Glance sessions kept stale captured values. Fix: widgets now use `PreferencesGlanceStateDefinition`; `syncDotCalWidgetState()` mirrors app DataStore widget prefs/theme/accent into per-widget Glance state, `provideContent` reads `currentDotCalWidgetSettings()`, and `WidgetUpdateWorker.updateNow()` syncs each active widget ID before `widget.update(context, id)`. Also disabled Widget Dot Texture control while Transparent Widgets is on, and date ring/pill interiors now use a solid surface color so transparent widgets do not show a solid red current-date circle. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed.
- 2026-07-04: Widget Pack + Config (Pro Backlog #2) completed. Added three separate picker entries/receivers without removing existing widgets: DotCal Countdown (`EventCountdownWidgetReceiver`, 2x2), DotCal Agenda (`AgendaListWidgetReceiver`, 4x2), and DotCal Month Grid (`MonthGridWidgetReceiver`, 4x4). All six widget providers now use `WidgetConfigActivity` (Compose, no XML UI) to choose All Calendars or a single calendar; per-widget selected account is stored in Glance preferences via `KEY_WIDGET_ACCOUNT_ID`, not Room schema. `WidgetDataRepository.load()` and `CalendarDao.getVisibleEventsForWidget()` accept optional `accountId` filters. Existing transparent/theme/accent/dot settings remain global and Pro-gated in Settings; per-widget calendar picker is Pro-only, with non-Pro falling back to All Calendars. Added RemoteViews-safe preview layout aliases for the new picker entries. No Room schema change. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed.
- 2026-07-05: Widget config screen top inset fix: `WidgetConfigActivity` root now applies `statusBarsPadding()` and `navigationBarsPadding()` so the title is not clipped under system bars.
- 2026-07-05: Countdown widget duplicate-layout fix. Root cause: `EventCountdownDotCalWidget` reused `DotCalWidgetSize.Small`, so `provideGlance()` rendered the same `SmallWidget` as the normal 2x2 widget. Added distinct `DotCalWidgetSize.Countdown` and `CountdownWidget()` renderer: centered `NEXT IN`, large countdown label, title, and time. Normal Small widget remains unchanged.
- 2026-07-05: Event Editor calendar selector added and refined from the pasted task prompt. `DotCalRepository.observeAssignableAccounts()` + `DotCalViewModel.assignableAccounts` expose only eligible manual-assignment calendars from `calendar_accounts`, excluding DotCal generated birthday/holiday rows and provider holiday/contact calendars (`#holiday@`, `#contacts@`, `addressbook#contacts`). Editor shows a compact Calendar pill above the title field; tapping opens the same dialog picker but with clean subtitles (`On this device`, `Google calendar`, `Device calendar`) instead of raw provider account names. New events default to the last selected account in this ViewModel session, else `isPrimary = 1`, else first by `sortOrder`; editing preserves the event's current account. Save passes selected `accountId` through `EventEditorData`; `saveLocalEvent()` writes it to existing `calendar_events.accountId` for new, edited, and detached recurrence events. No Pro gate. No Room/schema/DataStore change. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 20s).
- 2026-07-05: Recently-Deleted / restore (Pro Backlog #8) built. FREE feature, no Pro gate (data-safety; mirrors Google/Samsung/Apple where trash is free). No Room/schema/DataStore change. Design: `deleted_event_log` could NOT back this (it holds only sync tombstones `googleEventId`+`deletedAtMs`; local events aren't logged), so trash is a **file-based snapshot store**. New `data/trash/RecentlyDeletedStore.kt` (`DeletedSnapshot` = full `CalendarEvent` + its `EventReminder`s + `deletedAtMs`) writes one JSON file per deleted event to `filesDir/recently_deleted/{eventId}.json` via `org.json` (no new dependency); best-effort writes (a snapshot failure never blocks the delete); prunes entries >30 days on every `list()`. Repository: `deleteLocalEvent()` and `deleteTask()` now snapshot the master row + reminders before the hard delete (the recurring "This event" EXDATE path still does not snapshot — matches prior behavior). New `listRecentlyDeleted()`, `restoreDeleted(id)` (re-inserts event + reminders, reschedules still-future reminders, falls back to `LOCAL_ACCOUNT_ID` if the original account is gone), `purgeDeleted(id)`, `emptyRecentlyDeleted()`. VM: `recentlyDeleted` StateFlow + `refreshRecentlyDeleted()`/`restoreDeleted`/`purgeDeleted`/`emptyRecentlyDeleted`. UI in `ui/DotCalApp.kt`: new full-screen right-slide overlay `showRecentlyDeleted` hosting `RecentlyDeletedScreen`. Layout is an **iOS-style swipe list** (`SwipeableDeletedRow`): plain divided rows (no cards) with title + "Event/Task • when • deleted Xm ago"; swipe a row left to reveal Restore (brand-neutral fill) and Delete (red) action buttons; tap Restore to bring it back, tap Delete → `ConfirmDeleteDialog` → permanent purge. Only one row open at a time (`openRowId` hoisted in the screen; rows animate shut via a per-row `Animatable` offset + `draggable`). "Empty" action in the top bar clears all (confirm dialog). New Settings "Data" row "Recently Deleted" (below Import Calendar) opens it and refreshes the list. Added to `BackHandler` + `closeTopSurface`. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 33s), real `compileDebugKotlin`. Installed on device (Phone 3). No manual QA by agent.
- 2026-07-05: Backup & Restore (Pro Backlog #5) built. Pro-gated, no Room/schema/DataStore/manifest/permission change, no new dependency. New `data/backup/BackupSerializer.kt` — pure-Kotlin `object` using `org.json` (same approach as `RecentlyDeletedStore`); `BackupData(accounts, events, reminders, createdAtMs)`; `encode()`/`decode()` reuse the exact 23-field `CalendarEvent` mapping from `RecentlyDeletedStore` and add `CalendarAccount` (8 fields) + flat reminder rows (reminder `id` omitted → Room auto-gen PK). Envelope `{format:"dotcal-backup", version:1, ...}`; `decode` throws on missing/unknown `format` (foreign-file guard). Backs up only user-content tables (accounts referenced by exported events + `getAllUserEventsForExport()` masters + their reminders); `sync_metadata`/`deleted_event_log` excluded by design. Repository: `exportBackup()`, `importBackup()` (non-destructive merge — `insertAccountIfAbsent` for absent accounts preserving existing config, `upsertEvent` by id counting insert-vs-update, account-missing → `LOCAL_ACCOUNT_ID` fallback like `restoreDeleted`, reminders replaced+rescheduled only when the backup carries them for that event — matches ICS semantics), `BackupImportResult`. Reused existing DAO methods only — **no new DAO query**. VM: `exportBackup{}`/`importBackup{}` mirroring `exportIcs`/`importIcs`. UI in `ui/DotCalApp.kt`: SAF `CreateDocument("application/json")` + `OpenDocument()` launchers beside the ICS ones; two new Pro-gated Settings "Data" rows "Back Up Data" / "Restore Data" (non-Pro → Paywall, lock icon); default filename `dotcal-backup-<date>.json`; toast summaries. Paywall `PRO_FEATURES` gained "Backup & Restore". `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (3m 22s), real `compileDebugKotlin`. No phone/manual QA.
- 2026-07-05: Advanced Recurrence (Pro Backlog #7) built. No Room/schema/DataStore/manifest/permission change, no new dependency — reuses the existing `calendar_events.rrule` column. New `data/recurrence/RecurrenceRule.kt` — pure-Kotlin model + parser (`parse`/`toRRule`/`humanLabel`) for the subset the expander can honor: `FREQ` (DAILY/WEEKLY/MONTHLY/YEARLY) + `INTERVAL` (every N) + `BYDAY` (weekly multi-weekday `MO,WE,FR`; monthly single ordinal `2MO`/`-1FR`) + `COUNT` + `UNTIL`. **Backward-compat invariant:** a bare `FREQ=DAILY` parses to interval=1/no-extras and re-serializes byte-identical, so every existing event keeps working. Pure occurrence-date generation lives on the model (`blockAnchorDate`/`datesForBlock`/`fastForwardBlock`) walking the series in per-period "blocks". Engine (`DotCalRepository.expandRecurringEvents`/`expandRecurringTasks`) now routes through `RecurrenceRule.parse` + a single `expandRule()`: COUNT is counted from the series anchor (stable across scroll/views); the unbounded/UNTIL-only path fast-forwards to the visible range; EXDATE + per-occurrence timing still delegate to the untouched `occurrenceOn()`. Safety cap `MAX_RECURRENCE_BLOCKS = 10_000`. Removed the now-dead `expandDaily/Weekly/Monthly/Yearly`, `generateOccurrences`, and repo-local `dayOrNull/monthDayOrNull` (+`YearMonth` import). UI in `ui/DotCalApp.kt`: `RepeatChoiceSheet` rewritten — preset rows (added **Yearly** preset; engine always supported it) stay FREE, plus a Pro-gated **"Custom…"** row (non-Pro → Paywall via `onRequestPro`, lock icon). Pro tap opens new `CustomRecurrenceSheet` bottom sheet: frequency segmented control (reuses `TwoOptionSegmentedControl`), interval stepper (`CalcStepperButton`), weekday chips (weekly), day-of-month vs nth/last-weekday choice (monthly), and Ends = Never / On date (reuses `DateTimeChoiceSheet`) / After N. Live `humanLabel()` preview. `TaskEditorSheet` gained `isPro`/`onRequestPro` params (threaded from caller) so task recurrence gates the same way. Label readers `recurrenceDetailLabel()`, `quickAddRepeatLabel`, and both Repeat-row values now render `RecurrenceRule.parse(...)?.humanLabel()` instead of exact-match (advanced rules read correctly instead of showing "None"). ICS: exporter already wrote the full `RRULE` verbatim; `IcsParser.normalizeRrule` changed from FREQ-only flattening to `RecurrenceRule.parse(...)?.toRRule()` so INTERVAL/BYDAY/COUNT/UNTIL survive import too. Paywall `PRO_FEATURES` gained "Advanced Recurrence". `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (3m 11s), real `compileDebugKotlin`. No phone/manual QA.
- 2026-07-05: Advanced Recurrence Custom dialog layout fix after phone report. Root cause: `CustomRecurrenceSheet` used default partial `ModalBottomSheet` behavior and put Cancel/Done at the bottom of one tall scroll column, so buttons were initially hidden and state changes could collapse the sheet. Fix: `rememberModalBottomSheetState(skipPartiallyExpanded = true)`, 92% height sheet, scrollable form content, sticky bottom Cancel/Done action bar with navigation-bar padding. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 46s). Installed debug APK on device `4ab0d020`.
- 2026-07-05: App Lock + Private Vault (Pro Backlog #4) built. Pro-gated, no Room/schema/manifest/permission change, no new dependency. New `data/privacy/AppPrivacyManager.kt` stores app-lock state, salted SHA-256 PIN hash, and private event/task IDs in existing DataStore (`calendar_preferences`); no PIN plaintext is stored. App Lock screen appears over the app after launch/background when enabled, supports 4-8 digit PIN unlock plus Android system device credential via `KeyguardManager.createConfirmDeviceCredentialIntent()` when configured. Settings > Additional gained Pro row "App Lock & Private Vault"; privacy screen can set/change/remove PIN, enable/disable lock (disable/remove requires current PIN), and list/restore hidden events/tasks. Event Detail and Task Detail gained Move/Restore Private Vault actions. Private items are filtered out of normal event/task/agenda flows, task deep links, event deep links, and widgets; moving an item private cancels its existing reminders to prevent notification leaks, restoring reschedules still-future reminders. Paywall `PRO_FEATURES` gained "App Lock & Private Vault". `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed. No phone/manual UI QA.
- 2026-07-06: Private Vault divider polish after phone report. Root cause: vault rows reused `SettingsContentDivider`, which has a start inset for account/settings content rows, so the divider looked half-cut on the left. Added `PrivateVaultDivider` with full-width line under each private row. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed. Installed debug APK on device `4ab0d020`.
- 2026-07-06: ICS Import/Export + Backup & Restore ungated to FREE (data portability/safety; market norm — Simple Calendar and most offline apps give import/export free, and gating "restore my own data" earns 1-star reviews). UI-only change in `ui/DotCalApp.kt`: removed the `if (!isPro) showPaywall` gate from `onExportIcs`/`onImportIcs`/`onBackup`/`onRestore` (now launch SAF directly); the four Settings > Data rows (Export/Import Calendar, Back Up/Restore Data) pass `isPro = true` so they show the unlocked chevron and no Pro tag; removed "Import / Export" and "Backup & Restore" from Paywall `PRO_FEATURES`. No ViewModel/repository/serializer change (they never gated). No Room/schema/DataStore/manifest/permission change, no new dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 34s), real `compileDebugKotlin`. No phone/manual QA.
- 2026-07-06: Global Search (Next Feature Picks #1) built. **FREE**, no Pro gate, no Room/schema/DataStore/manifest/permission change, no new dependency, no version bump. Read-only path only: new `CalendarDao.searchUserEvents(q)` (`@Query` LIKE over title/description/location, joins `calendar_accounts` for `isVisible = 1`, excludes `source IN ('BIRTHDAY','HOLIDAY')`, returns master rows — recurring events appear once, not per-occurrence). `DotCalRepository.searchItems(query)` trims/blank-guards, calls the DAO, drops Private Vault items via the existing `filterOutPrivate` helper. VM: `_searchResults` StateFlow + `search()`/`clearSearch()` (mirrors `recentlyDeleted`). UI in `ui/DotCalApp.kt`: new `Icons.Default.Search` icon in `CalendarActionBar` (optional `onSearch`, threaded through `CalendarTabContainer`; Tasks bar unaffected), FREE so no Pro gate. New full-screen right-slide overlay `showSearch` hosting `SearchScreen`: auto-focused search field (debounced 220ms → `viewModel.search`), in-memory facet chips — type (All/Events/Tasks), date preset (Any time/Upcoming/Past/This month), calendar (from `assignableAccounts`) — filtering the fetched results without re-query. Results list reuses `EventRow` for events + new lightweight `SearchTaskResultRow` for tasks (TaskRow's swipe/complete machinery is wrong for search). Tap event → `openEventDetail`; tap task → `taskDetail`. Empty/no-result hint states. `showSearch` added to `closeTopSurface` + `BackHandler`; close/open clears search. New private composables: `SearchScreen`, `SearchFilterChip`, `SearchHintBox`, `SearchTaskResultRow`, enums `SearchTypeFilter`/`SearchDatePreset`. Added `Icons.Default.Search` import. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 48s), real `compileDebugKotlin` + `kspDebugKotlin` (Room validated the new query). No phone/manual QA.
- 2026-07-06: App Lock leak fix + Month view Upcoming strip (both in `ui/DotCalApp.kt`, no Room/schema/DataStore/manifest/permission change, no new dependency, no version bump). (1) BUG: with App Lock enabled, an open `ModalBottomSheet`/dialog stayed visible over the PIN screen because those render in their own window, above the `AppLockScreen` composable (which is just a Box child). Fix: the ON_STOP re-lock branch now also dismisses the window-based transient surfaces (`showSheet`, `showTaskEditor`, `editingTask`, `pendingDelete`, `pendingTaskDelete`) so no event content floats over the lock. Full-screen right-slide overlays (search/paywall/editor/etc.) were never affected — they compose before the lock overlay in the same Box and are covered. (2) FEATURE: Month view now fills the empty space under the 6-row grid with an **Upcoming** strip (weighted below the grid): a divider, an "UPCOMING" label, then upcoming events (today onward, `isTask == 0`, from the existing `agendaEvents`/`observeUpcomingAgendaEvents` flow) grouped by day and rendered with the SAME `AgendaDateHeader` + `AgendaEventCard` used by the Agenda tab (tap → `openEventDetail`); empty state "Nothing upcoming". The strip is static/upcoming — it is NOT tied to the tapped date. Day-tap in month view keeps its existing behavior (`showSheet = true` → `EventListSheet` bottom sheet with that day's events + Add). `MonthView` gained `upcomingEvents` + `onEventClick` params. (Design note: an earlier pass made the strip a selected-day list that replaced the bottom sheet; per user feedback it was changed to a static Upcoming list with the day-tap sheet retained.) The App Lock fix still guards the retained bottom sheet on re-lock. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 28s), real `compileDebugKotlin` + `kspDebugKotlin`. No phone/manual QA by agent.
- 2026-07-06: Month view Upcoming strip **REMOVED** (user feedback — didn't like any variant: too much space/gaps, then compact rows, then compact cards). `MonthView` reverted to grid-only: dropped the `upcomingEvents`/`onEventClick` params, the `upcomingByDate` grouping, the whole below-grid strip Column, and the strip-only composables (`MonthUpcomingDayHeader`, `MonthUpcomingRow`) + `CalendarEvent.monthUpcomingTimeLabel()` helper. Call site in the `CalendarTab.Month` branch updated to match. Day-tap bottom sheet (`showSheet` → `EventListSheet`) is pre-existing and retained. The App Lock ON_STOP leak fix is independent and still in place. `agendaTimeRange()` verified intact (all-day guard present). `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 8s), real `compileDebugKotlin` + `kspDebugKotlin`. Installed on device `4ab0d020`. No phone/manual QA by agent. NOTE: if the Month empty-space fill is revisited, the user wants a card-based design, no "UPCOMING" heading, no divider between grid and content.
- 2026-07-06: Settings **Sync Now** row typography aligned to the rest of Settings. Root cause: `SettingsSyncNowRow` used a 14sp/SemiBold title + 11sp subtitle while every other row (`SettingsMenuRow`/`SettingsImportExportRow`/`SettingsProBadgeRow`) uses 16sp/Normal title + 12sp subtitle, so "Sync Now" rendered smaller and bolder than its neighbors. Changed title to 16sp `FontWeight.Normal`, subtitle to 12sp, dropped the stray `lineHeight = 13.sp`. Same `mono`/colors/layout. `:app:assembleDebug` passed (1m 56s). Not installed.
- 2026-07-07: **Event & Task Templates (Next Feature Picks #2) built. PRO.** No Room/schema/DataStore/manifest/permission change, no new dependency, no version bump. File-based store (schema stays 5-table locked): new `data/templates/EventTemplateStore.kt` = `EventTemplate` model (date-agnostic: name, isTask, title, description, location, accountId, isAllDay, startMinuteOfDay, durationMinutes, reminderMinutes, rrule, createdAtMs) + JSON store, one file per template under `filesDir/event_templates/{id}.json`, best-effort writes, mirrors `RecentlyDeletedStore`. Repository: `listTemplates()`/`saveTemplate()`/`deleteTemplate()`. VM: `templates` StateFlow + `refreshTemplates()`/`saveTemplate()`/`deleteTemplate()` (mirrors `recentlyDeleted`). UI in `ui/DotCalApp.kt`:
  - **Save**: PRO-gated "Save as template" text action added to `EventEditorScreen` (new params `templatePrefill`/`onSaveTemplate`) and `TaskEditorSheet` (same). Non-Pro tap → Paywall. Tap → `TemplateNameDialog` (name prefilled with title) → builds `EventTemplate` from current editor fields → `viewModel.saveTemplate` → toast. Event captures title/desc/location/account/all-day/start-time/duration/reminder/rrule; task captures title/time/reminder/rrule. (Color intentionally omitted — the event editor has no color field, so `EventEditorData` can't round-trip it.)
  - **Use**: long-press the top-bar **+** (Add) icon → opens the Templates overlay (non-Pro → Paywall). Add button changed from `IconButton` to a `combinedClickable` Box (needs `@OptIn(ExperimentalFoundationApi::class)` on `CalendarActionBar` — combinedClickable is experimental in Compose BOM 2024.12.01); plain tap = unchanged blank add. `onAddLongPress` threaded through `CalendarTabContainer`. Tapping a template applies it to the current `selectedDate`: event templates seed `EventEditorScreen` (via `templatePrefill` — the editor's `event==null` seed block now folds template fields in alongside the existing QuickAdd `prefill` path), task templates route to `TaskEditorSheet` (via `templatePrefill`). User reviews and Saves through the normal path — no new save/validation/repo logic.
  - **Manage**: new full-screen right-slide overlay `showTemplates` → `TemplatesScreen` (cards: name + "Event/Task · time · duration · repeats"; tap card = use; trash icon → `ConfirmDeleteDialog` → delete; empty state explains "Save as template"). Reached from + long-press AND Settings > Data > **Templates** row (`SettingsProBadgeRow`, PRO tag for non-Pro → Paywall). Added to `BackHandler` + `closeTopSurface`. `templatePrefill`/`taskTemplatePrefill` state cleared on every normal add/edit open, dismiss, and save so a template never leaks into a plain "+" add.
  - Paywall `PRO_FEATURES` gained "Event & Task Templates". New composables: `TemplatesScreen`, `TemplateCard`, `TemplateNameDialog`, helpers `templateSummaryLabel`/`formatDurationShort`. New imports: `combinedClickable`, `ExperimentalFoundationApi`, `DeleteOutline`.
  - `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 32s), real `compileDebugKotlin` + `kspDebugKotlin`. No phone/manual QA by agent. Not installed.
- 2026-07-07: Installed the current debug build (Templates + Sync Now fix) on device `4ab0d020` at user request.
- 2026-07-07: **Task Time Blocking (Next Feature Picks #3) built. FREE.** No Room/schema/DataStore/manifest/permission change, no new dependency, no version bump. Convert a task into a calendar time-block reusing the existing event model + editor. `TaskDetailScreen` gained an **"Add to Calendar"** action (shown only for open tasks, above Move to Private Vault). New `blockFromTask(task)` helper in `ui/DotCalApp.kt` builds a `QuickAddResult` from the task (title; date = task's due date or `selectedDate`; start = task's due time or 9am; 1-hour block; timed, no recurrence) and opens the standard `EventEditorScreen` via the existing QuickAdd prefill path (`openQuickAddResult`). User reviews/adjusts the slot and Saves through the normal `saveEvent` path → creates a real `calendar_events` event (`isTask=0`). The original task is **left intact** (non-destructive — no delete/complete side effect). No Pro gate. `:app:assembleDebug` passed (real `compileDebugKotlin`).
- 2026-07-07: **Event & Task Templates entry points redesigned** (user feedback: the old "Save as template" bottom link + hidden long-press-`+` were confusing). All in `ui/DotCalApp.kt`, no store/VM/repo change. (1) **Use/browse**: removed the hidden **long-press-`+`** gesture; `CalendarActionBar` now has a **three-dots (⋮) overflow menu** at top-right. The standalone Quick Add sparkle icon was **moved into the same ⋮ menu** — items are **"Quick Add"** and **"Templates"**, each rendered by the new `ActionBarMenuItem` composable (accent icon chip + label + subtitle, mono font, rounded 16dp menu, 216dp width). Templates opens `TemplatesScreen` (non-Pro → Paywall via `onTemplates`); Quick Add behavior unchanged. The `+` reverted to a plain `IconButton` (dropped `combinedClickable`/`ExperimentalFoundationApi` — imports removed). `onAddLongPress` param renamed to `onTemplates` on `CalendarActionBar` + `CalendarTabContainer`; `onQuickAdd` still threaded. (2) **Save**: removed the faint bottom **"Save as template"** text link from both `EventEditorScreen` and `TaskEditorSheet`. Replaced with a **⋮ overflow menu → "Save as template"** — in the Event editor top bar and the Task editor title row — shown **only when editing an existing event/task** (`event != null` / `task != null`); the **Add (new) event/task screen stays clean** (no ⋮), per user request. Same `showSaveTemplateDialog` + Pro-gate logic as before. New icon import `MoreVert`. Settings › Data › Templates row unchanged (secondary entry). `:app:assembleDebug` passed (exit 0, real compile). Not re-installed.
- No phone/manual UI QA run.
- 2026-07-07: **Calendar Sets / Focus Profiles (Next Feature Picks #4) built. PRO.** No Room/schema change (still 5 tables), no new dependency, no version bump. Lets a user save a named snapshot of which calendars are currently visible ("Work", "Personal", "Family") and switch between them instantly. New file-based JSON store `data/profiles/FocusProfileStore.kt` (`FocusProfile(id, name, accountIds, createdAtMs)`), same pattern as `EventTemplateStore`/`RecentlyDeletedStore` — one JSON file per set under `filesDir/focus_profiles/{id}.json`, best-effort writes, unreadable files pruned on `list()`. `DotCalRepository` gained `listFocusProfiles()` / `saveFocusProfile()` / `deleteFocusProfile()` / `applyFocusProfile(id)`; `applyFocusProfile` reuses the existing `dao.getAccountsForWidgetConfig()` + `dao.updateAccountVisibility` (skips the local account, only toggles accounts whose visibility actually differs from the target) then calls `updateWidgets()`. `DotCalViewModel` mirrors this with a `focusProfiles` StateFlow + matching functions. UI lives in `ui/DotCalApp.kt`: new **Settings › Data › "Calendar Sets"** `SettingsProBadgeRow` (non-Pro → Paywall), new `showFocusProfiles` overlay state wired into `closeTopSurface()`/`BackHandler`, and new `FocusProfilesScreen`/`FocusProfileCard` composables (list of sets — tap to apply, trash icon to delete with confirm — plus a bottom "Save Current as Set" pill that snapshots the currently-visible `accounts` into a new `FocusProfile`). Reused/generalized two existing composables instead of duplicating: `TemplateNameDialog` gained an optional `title` param (existing call sites unaffected), `CalendarAddAccountRow` gained an optional `label` param (existing "Add Account" call site unaffected). Paywall `PRO_FEATURES` list gained a "Calendar Sets" entry. `:app:assembleDebug` passed — **BUILD SUCCESSFUL in 4m 31s**, real (non-cached) `kspDebugKotlin`/`compileDebugKotlin` confirmed. No phone/manual UI QA run.
- 2026-07-07: Installed the current debug build (Calendar Sets / Focus Profiles) on device `4ab0d020` at user request.
- 2026-07-07: **Fix (Calendar Sets):** the "Save Current as Set" pill was invisible on device — both the empty-state `Column` and the `LazyColumn` in `FocusProfilesScreen` used `Modifier.fillMaxSize()`, which consumed all vertical space and pushed the trailing `CalendarAddAccountRow` off the bottom of the screen. Changed both to `Modifier.weight(1f).fillMaxWidth()` so the save pill stays anchored at the bottom. Rebuilt (`:app:assembleDebug`, 2m 32s, real compile) and reinstalled on `4ab0d020`.
- 2026-07-08: **Shift Pattern Builder + Bulk Apply built. PRO.** No Room/schema/DataStore/manifest/permission change, no new dependency, no version bump. Real user feedback: emergency-services rota with 4 on (2 days + 2 nights) / 4 off was too painful to enter day-by-day. New file-based store `data/shifts/ShiftPatternStore.kt` (`ShiftType`, `ShiftPattern`, `ShiftGenerationRecord`) stores shift types under `filesDir/shift_types/`, patterns under `filesDir/shift_patterns/`, and generated event IDs under `filesDir/shift_generations/`; unreadable files are pruned on list, writes are best-effort. Pure `expandShiftPattern()` handles modulo cycle math across any date range. Repository/VM gained shift type/pattern CRUD and `applyShiftPattern()`, which replaces overlapping generated records before regenerating so shifts do not duplicate; generated shifts save through existing `saveLocalEvent()` and reuse existing `calendar_events` rows/reminders/widgets. `EventEditorData` gained optional `colorHex` so generated shift colors can be stored in the existing column. UI in `ui/DotCalApp.kt`: Settings > Data > **Shift Patterns** row (non-Pro -> Paywall), `showShiftPatterns` full-screen right-slide overlay, shift type dialog, pattern builder, generate dialog (default calendar + months ahead), and Paywall `PRO_FEATURES` entry. Bulk apply/paint: Month view long-press enters multi-select (Pro-gated), taps toggle dates, bottom bar opens a compact template picker; selecting an event template stamps one-off events onto selected dates via `applyTemplateToDates()` (rrule intentionally not copied). `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (3m 25s), real compile. No phone/manual QA, not installed.
- 2026-07-08: **Shift Pattern Builder polish:** added picker UI missing from first build. Shift type editor now has a tappable color swatch row that opens the existing HSV/hex color picker (`CustomAccentPickerDialog`, title = "Shift Color") while keeping the hex field for precision. Pattern builder now uses the existing wheel date picker for **Start date** instead of raw ISO text. Generate dialog now has a **Generate from** date picker and passes the selected start date into `applyShiftPattern()`; default = pattern cycle start date. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (3m 24s), real compile. Not installed.
- 2026-07-08: **Shift Pattern Builder bugfix after phone QA:** shift types are now editable by tapping their row; edit preserves the existing `ShiftType.id` so patterns keep referencing the same type. Removed the redundant `Color hex` input from the visible editor now that the color picker exists. Generate dialog now defaults **Generate from** to today (pattern math still uses the saved cycle start date for alignment), which avoids accidentally generating an old/invisible range. Zero-result toast now says to check that the pattern uses Day/Night shift types and not only Off. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (3m 13s), then installed on device `4ab0d020`.
- 2026-07-08: **Week/Day timeline clipping fix after phone QA:** timed events in Week view and Day view now render in full-height day timeline overlays instead of being drawn only inside their start-hour row. Fixes 07:00-19:00 shift showing as a small block only at 07:00 and overlapping 09:00-10:00 event showing as a separate half chip. Week view now uses `WeekTimeColumn` + `WeekDayColumn`; Day view uses `DayTimeColumn` + `DayTimelineColumn`; both compute top offset from start time and height from `durationMinutes()`, while `layoutTimedEvents()` still handles overlap columns. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (3m 27s), then installed on device `4ab0d020`.
- 2026-07-08: **Week/Day timeline polish after user QA:** time labels are now compact two-digit hours (`00`, `01`, ... `23`) instead of `00:00`, reducing the gutter in Week and Day timelines. Week/Day timelines now include bottom scroll clearance so the 23:00 section can scroll above the floating bottom nav instead of hiding behind it. Day view no longer shows the empty bottom `Tasks` / `No tasks` footer; it still shows actual tasks for the selected day when they exist. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (2m 55s). Not installed.
- 2026-07-08: Installed the latest debug build (Week/Day compact time labels + bottom clearance + Day empty-task-footer removal) on device via `C:\Users\Admin\AppData\Local\Android\Sdk\platform-tools\adb.exe install -r app\build\outputs\apk\debug\app-debug.apk` (`Success`).
- 2026-07-08: **Week/Day 24-hour box cleanup after user QA:** fixed the bottom-clearance implementation so the actual timeline grid stays exactly inside the 24-hour box (`00` through `23`) and the nav clearance is blank space after the box. This prevents hour separator lines from continuing below `23` or under the floating bottom nav. Applied to both Week and Day timelines. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (3m 23s). Not installed.
- 2026-07-08: **Day view date navigation polish:** added a compact Day header above the timeline: left chevron, centered `EEE DD MMM` label with year subtext, and right chevron. Chevrons use the existing `onPreviousDay` / `onNextDay` callbacks; tapping the center jumps to today via existing `onJumpToday`; horizontal swipe on Day view now switches previous/next day using the same threshold pattern as Week/Month. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (3m 10s), then installed via full SDK adb path (`Success`).
- 2026-07-08: **Week/Day fixed bottom boundary after user QA:** changed Week and Day timeline scroll areas to use a fixed bottom overlay (`TimelineBottomBoundary`) above the floating bottom nav. The timeline content still scrolls, but the `after 23` boundary line stays fixed as the end of the visible time box and content only moves up to that line; the clearance below it is blank/static. Removed the scrolling final separator from timeline draw loops (`repeat(23)` internal separators only). Applies to both Week and Day. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (4m), then installed via full SDK adb path (`Success`).

Known UX note from user QA:
- Day view now shows exact selected day and supports chevrons + horizontal swipe for previous/next day. If user wants more polish later, consider improving the global top title format for Day view too; current in-view header is intentionally compact to preserve vertical space.

Refactor note:
- `ui/DotCalApp.kt` is now very large (~12k lines) and contains app root state, calendar views, editors, settings, templates, shift patterns, search, privacy, onboarding, and helpers. Recommend a low-risk mechanical split after current QA stabilizes: extract calendar view composables (`MonthView`, `WeekView`, `DayView`, `AgendaPreview`, `YearView`, shared timeline layout) first, then Settings feature screens, then editor/detail screens. Keep behavior identical, no new dependencies, no schema changes; build after each split.
- 2026-07-09: Refactor/optimization plan created at `Docs/DOTCAL_REFACTOR_PLAN.md`. Status: planning done only; app code not changed. Next refactor step is the mechanical Calendar Views split, preserving behavior/UI exactly and building immediately after the app-code move.
- 2026-07-09: **Refactor Step 1 complete: Calendar Views split.** New `ui/CalendarViews.kt` contains Month/Week/Day/ThreeDay/Year views, mini-month canvas, and shared timeline layout helpers. `DotCalApp.kt` reduced from 12,969 to 11,905 lines. Behavior/UI intended unchanged; package visibility only widened for shared helpers/types needed across files (`DotCalPalette`, `mono`, timeline constants, `CalendarEvent` date/time/color helpers, `noRippleClickable`). `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 3m 32s). No phone/manual UI QA run. Next refactor step: shared UI shell/theme split.
- 2026-07-08: **Refactor Step 2 complete: Shared UI shell/theme split.** New `ui/DotCalTheme.kt` contains palette/theme/accent types and helpers. New `ui/AppChrome.kt` contains system bar sync, calendar top bar, overflow menu, segmented calendar view control, bottom nav, tab enums, and `noRippleClickable`. `DotCalApp.kt` reduced from 11,905 to 11,211 lines. Behavior/UI intended unchanged; package visibility only widened for moved shared code. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed after theme split (BUILD SUCCESSFUL in 3m 10s) and after chrome split (BUILD SUCCESSFUL in 2m 46s). No phone/manual UI QA run. Next refactor step: Event Detail and Event Editor split.
- 2026-07-09: **Refactor Step 3 complete: Event Detail and Event Editor split.** New `ui/EventScreens.kt` contains Event Detail, Event Editor, image/voice attachment/editor pieces, recurrence/date/reminder picker sheets, and shared bottom-sheet picker chrome. `DotCalApp.kt` reduced by roughly 2.2k lines. Behavior/UI intended unchanged; package visibility only widened to `internal` for helpers shared across the split and existing Task/Settings surfaces. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 3m 2s), then installed via full SDK adb path (`Success`).
- 2026-07-09: **Refactor Step 4 complete: Tasks split.** New `ui/TaskScreens.kt` contains Task Detail, Tasks list, task top chrome/filter rows, task row/metadata/empty state, Task Editor sheet, and Task Time picker sheet. Behavior/UI intended unchanged; Task Time Blocking still opens the normal Event editor from Task Detail and leaves the original task intact; task recurrence still uses the existing Pro-gated Repeat picker. Package visibility only widened for shared task/date/header helpers needed across the split. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 2m 22s), then installed via full SDK adb path (`Success`).
- 2026-07-09: **Refactor Step 5 complete: Settings and Pro feature screens split.** New `ui/SettingsScreens.kt` contains Settings root/preview, theme/accent UI, app lock/private vault settings, account/settings rows, sync rows, widget toggles, global holidays, and privacy policy. New `ui/ProFeatureScreens.kt` contains Paywall, Search, Quick Add, Templates, Calendar Sets, Shift Patterns, Recently Deleted, and Date Calculator surfaces. `DotCalApp.kt` reduced to 3,638 lines and now mostly holds root composition, state, launchers, overlays, and shared helpers. Behavior/UI intended unchanged; package visibility only widened for helpers reused across extracted files. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 2m 48s), then installed via full SDK adb path (`Success`).
- 2026-07-09: **Refactor Step 6 complete: Onboarding split.** New `ui/OnboardingScreens.kt` contains onboarding page model/list, onboarding screen/copy/color models, progress/hero UI, and onboarding draw-scope helpers. `DotCalApp.kt` reduced to 2,800 lines and keeps root onboarding state, permission launchers, and deep-link onboarding skip behavior. Behavior/UI intended unchanged; only package visibility changed for onboarding symbols needed by root state. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 2m 44s), then installed via full SDK adb path (`Success`).
- 2026-07-09: **Refactor Step 7 complete: Root App Cleanup.** New `ui/AgendaScreens.kt` contains Agenda preview/card/date-header/empty-state UI and the day event-list sheet. New `ui/DialogScreens.kt` contains shared delete/template/update dialogs. New `ui/UiModels.kt` contains shared UI enums/constants/formatters. New `ui/UiHelpers.kt` contains shared date/time, account-label, media, recurrence-label, color, and Android context helpers. `DotCalApp.kt` reduced to 1,962 lines and is now focused on root composition, state wiring, launchers, overlay orchestration, and major-surface routing. Behavior/UI intended unchanged; no overlay boolean/close-order/navigation changes. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 3m 10s), then installed via full SDK adb path (`Success`).
- 2026-07-09: **Refactor Step 8 complete: Low-Risk Optimization Pass + themed toasts.** Added stable lazy keys for Month grid days and Day all-day rows, remembered Search facet filtering, remembered Quick Add example chips, and cached Tasks reminder lookup by event id instead of scanning reminders per row. Added shared `showDotCalToast(...)` so app toasts use DotCal palette colors (white in light theme, dark in dark theme), then switched visible root/paywall/event/task feedback to it. `DotCalApp.kt` is 1,967 lines after the small toast boot-palette wiring. Behavior/UI intended unchanged except requested toast theme background. No Room/schema/DataStore/manifest/permission change, no dependency, no version bump. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 2m 55s), then installed via full SDK adb path (`Success`).
- 2026-07-09: **Settings Theme row value polish.** Fixed the Settings > Additional > Theme right-side value text that showed an encoded separator after file moves. It now shows a clean ASCII label like `System / Red`, and `SettingsMenuRow` title/value text is constrained to one line with ellipsis so rows do not crowd or overflow. No behavior/storage/schema/dependency change. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 2m 34s), then installed via full SDK adb path (`Success`).
- 2026-07-09: **Mojibake sweep.** Scanned app source/resources for common mojibake signatures and cleaned user-facing strings in Privacy Policy, import/restore/purchase toasts, recurrence labels, voice permission text, agenda time ranges, and custom-repeat steppers. Replaced broken bullets/dashes/ellipsis/minus signs with ASCII-safe text. No behavior/storage/schema/dependency change. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 3m 10s), then installed via full SDK adb path (`Success`).
- 2026-07-08: **Search filter UI compacted after user feedback.** Search screen now shows three compact dropdown selectors below the search field (`Type`, `Time`, `Calendar`) instead of wrapping chip rows for all/events/tasks, date presets, and every calendar. Behavior/filter logic unchanged; only the filter controls changed. `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug` passed (BUILD SUCCESSFUL in 3m 1s). No phone/manual UI QA run.
- 2026-07-09: User preference update: after successful debug builds, install the debug APK on the connected phone unless the user explicitly says not to. Current debug APK installed via full SDK adb path (`C:\Users\Admin\AppData\Local\Android\Sdk\platform-tools\adb.exe install -r app\build\outputs\apk\debug\app-debug.apk`) with result `Success` after Refactor Step 8 / themed-toast build.

Current dirty files may include earlier Pro/UI polish and release assets. Do not revert unrelated user/local changes.

## Current Next Step

DONE (2026-07-05): `1.1.3` / `versionCode 9` uploaded to Play Internal Testing by user.

Google billing/payment review is clear as of 2026-07-09 per user. Next local action is Play billing verification on an internal-testing install. Advanced Reminder Profiles (Next Feature Picks #5, PRO) remains NOT started; do not start without explicit confirmation. Offline OCR remains a possible later Pro Backlog pick if user asks.

Play billing verification:
1. Confirm test Gmail is in `pro-tester`.
2. Log into that Google account on test device/emulator.
3. Open Settings > DotCal Pro. Paywall should open and show live INR 149 from `ProductDetails`. If fallback appears, debug product-detail loading first.
4. Tap Buy Pro. Confirm Play test purchase sheet appears as test order, no real charge.
5. Confirm purchase. Verify image/voice gates removed, Large widget shows content, Date Calculator opens, Settings says Pro active.
6. Test Restore Purchase on fresh install.
7. Complete Content Rating + Data Safety, then Open Testing.

## What To Test Now

Refactor Step 8 debug build:
- Toast theme regression: in light theme, toasts should use white background; in dark theme, toasts should use dark background with readable text.
- Onboarding flow: Welcome, Calendar Access, Reminders, Birthdays, Ready; optional permission buttons; Skip/Back; deep links still skip onboarding.
- Calendar Agenda view and day-tap event list sheet: empty state, Add Event, event row tap to detail.
- Settings root and sub-screens: Theme, Calendar Accounts, Add Account, Global Holidays, App Lock & Private Vault, Privacy Policy.
- Pro/full-screen surfaces: Paywall, Search, Quick Add, Templates, Calendar Sets, Shift Patterns, Recently Deleted, Date Calculator.
- Regression spot-check from Step 4: Tasks tab, Task Detail, Task Editor, and Add to Calendar still behave as before.

Play/Internal-testing build only, not debug sideload:
- Settings > DotCal Pro opens Paywall; app must not close.
- Pro-gated image/voice/date-calculator taps open Paywall; app must not close.
- Paywall X dismisses; Restore Purchase shows correct toast/snackbar.
- Large widget locked state shows rounded Unlock button; tapping opens Paywall.
- After Pro purchase/restore: image + voice work, Date Calculator opens, Large widget unlocks.
- In-app update dialog still works when older installed build sees newer track build.
- Event Editor: Calendar pill appears above title; new event defaults to primary/last selected calendar; editing preserves/updates calendar; Holiday/Birthday/contact provider calendars do not appear; selected-calendar event appears when widget is configured to that calendar.
- Backup & Restore (Settings > Data > Back Up Data / Restore Data, PRO): non-Pro tap opens Paywall (lock icon shown); Pro tap Back Up Data → SAF create `dotcal-backup-<date>.json`, toast "Backup saved"; Restore Data → pick that file → toast "Restored: N new, M updated", restored events/tasks appear with reminders rescheduled; restoring over existing rows updates them and never deletes unrelated live data; picking a non-DotCal/foreign JSON → toast "Restore failed — not a valid DotCal backup", no crash.
- Recently Deleted (Settings > Data > Recently Deleted, FREE, iOS-style swipe list): delete an event and a task, confirm both appear; swipe a row left to reveal Restore/Delete; Restore brings it back into the calendar/tasks with reminders rescheduled; Delete (red) → confirm → permanent purge; only one row opens at a time (opening another closes the previous); "Empty" clears all; deleting a recurring "This event" occurrence does NOT add a trash entry (it's an EXDATE, not a real delete).
- Advanced Recurrence (Event/Task editor > Repeat, Custom builder is PRO): existing recurring events (bare `FREQ=DAILY/WEEKLY/MONTHLY`) still expand exactly as before (backward compat — verify no shift/duplication). Preset rows None/Daily/Weekly/Monthly/**Yearly** are FREE. Non-Pro tap "Custom…" → Paywall (lock icon). Pro: build "Every 2 weeks on Mon, Fri" → Repeat row + Event Detail show the human sentence, occurrences land on the right days; monthly "on the 3rd Tuesday" and "last Friday" land correctly and short months skip gracefully; Ends = After N shows exactly N occurrences (stable when scrolling months); Ends = On date stops on/before that date; ICS Export then Import round-trips the custom rule (INTERVAL/BYDAY/COUNT/UNTIL preserved, not flattened to bare FREQ).
- App Lock + Private Vault (Settings > Additional > App Lock & Private Vault, PRO): non-Pro row opens Paywall. Pro: set a 4-8 digit PIN, leave/reopen app and verify lock screen appears; wrong PIN stays locked; correct PIN unlocks; Use Device Lock works when Android secure lock is configured. Disable/remove PIN requires current PIN. Event Detail/Task Detail > Move to Private Vault hides item from Calendar, Agenda, Tasks, widgets, and deep links; reminders for hidden items should not fire; Privacy screen lists hidden items and Restore brings them back with future reminders rescheduled.
- Event & Task Templates (PRO, entry points redesigned 2026-07-07): **Use** = Calendar top-bar **⋮ overflow → Templates** (non-Pro → Paywall). **Save** = **⋮ overflow → Save as template** in the top-right of the Event editor / title row of the Task editor, shown **only when editing an existing** event/task (the Add screen has no ⋮). Verify: the old bottom "Save as template" link is gone; the old long-press-`+` no longer opens templates (plain `+` = blank add). Pro: edit an event → ⋮ → Save as template → name → toast; same in Task editor. Calendar ⋮ → Templates screen; tap a template → editor opens pre-seeded to the selected date (event → Event editor, task → Task editor) → Save via normal path lands it correctly (title/desc/location/calendar/all-day/time/duration/reminder/recurrence carry over; task carries title/time/reminder/recurrence). Settings > Data > Templates still lists/deletes; empty state explains how to create. Templates persist across restarts (`filesDir/event_templates/`).
- Task Time Blocking (FREE): open a task's detail → **Add to Calendar** appears (open tasks only, not completed). Tap → Event editor opens pre-seeded (title from task; date = task's due date or the selected date; start = task's due time or 9am; 1-hour block; timed). Adjust the slot → Save → a real calendar event appears on that day; the **original task still exists** in Tasks (unchanged). A task with no due date seeds today/selected date at 9am. Completed tasks show no "Add to Calendar" action.
- Shift Patterns + Bulk Apply (PRO): Settings > Data > **Shift Patterns** (non-Pro → Paywall). Pro: create shift types `Day` (07:00, 12h), `Night` (19:00, 12h), and `Off`; verify tapping an existing shift type opens edit, Day/Night color swatch opens the color picker, and there is no redundant `Color hex` field. Build an 8-day cycle `Day, Day, Night, Night, Off, Off, Off, Off`; verify **Start date** opens the wheel date picker. Generate 6 months to a chosen calendar; verify **Generate from** defaults to today, opens the date picker, and generation starts from the selected date. Expected result: non-zero toast when the cycle includes Day/Night; if the cycle is only Off/missing types, toast explains no shifts were added. In Week and Day views, a `Day` shift from 07:00-19:00 must render as one full-height 12-hour block spanning 07-19; a separate 09:00-10:00 event on the same date must render in its own overlap column at 09-10, not as half chips at start hours. Verify Week/Day time labels show compact `00`-style hours (not `00:00`), and that the `23` section can scroll above the floating bottom nav. Day view should not show an empty `Tasks` / `No tasks` footer, but should show task rows when that day has tasks. Spot-check no drift across month boundaries/DST and that night shifts end next day. Regenerate the same range → prior generated event IDs are replaced, not duplicated. Delete pattern → generated events are removed. Month view: long-press enters date selection, tap 5 scattered dates, Apply Template with an event template → 5 one-off events created; template recurrence is NOT copied.

## Resume Prompt

You are continuing development of DotCal (`com.dotfield.dotcal`).

Workdir: `D:\Caveman\caveman\Nothing-Calendar`.

Read `Docs/HANDOFF.md` first — it is source of truth. Respect Hard Rules, Pro/Billing status, schema invariants, and Current Next Step.

**Latest status (2026-07-09):** Branch `pro-features`. Build `versionCode 9` / `versionName 1.1.3`, `:app:assembleDebug` passing. Latest local work **installed on device**: Refactor Step 8 Low-Risk Optimization Pass + themed toasts.

Latest local feature: **Shift Pattern Builder + Bulk Apply (PRO)** plus phone-QA fixes. Files: `data/shifts/ShiftPatternStore.kt`, `DotCalRepository.kt`, `DotCalViewModel.kt`, `ui/DotCalApp.kt`. No Room/schema/DataStore/manifest/permission change, no new dependency, no version bump. Settings > Data > Shift Patterns opens the Pro-gated builder; shift type rows are editable; Shift type color swatch opens a color picker with no visible hex field; Pattern Start date and Generate from use the wheel date picker; Generate from defaults to today; Week/Day timed events now span their real duration; Week/Day time labels use compact `00` style; Week/Day use a fixed bottom boundary line after `23` with blank static clearance above the floating nav; Day view has a compact selected-date header, chevrons, and horizontal swipe for day switching; Day view hides the empty Tasks footer but shows actual selected-day tasks; Month long-press enters Pro-gated bulk date selection and stamps existing event templates as one-off events.

Kept from prior sessions: App Lock ON_STOP leak fix; Global Search ("fine"); ICS Import/Export + Backup & Restore FREE. Month view is grid-only (Upcoming strip removed — if revisited, user wants card-based, no "UPCOMING" heading, no divider; day-tap `EventListSheet` retained).

Done recently: **Calendar Sets / Focus Profiles (#4)** built, installed, committed/pushed earlier; **Shift Pattern Builder + Bulk Apply** built locally from shift-worker feedback; **Refactor Steps 1-8** split calendar views, shared chrome/theme, event detail/editor screens, task screens, settings, Pro feature screens, onboarding, agenda/dialog/common helpers, root app cleanup, and low-risk optimization/themed-toast cleanup. Advanced Reminder Profiles (#5) remains NOT started; do not start without confirmation.

Build/install: `versionCode 9`, `versionName 1.1.3`, `:app:assembleDebug` passed after Refactor Step 8 (2m 55s). Latest debug APK installed successfully on device via full SDK adb path.

STRICT:
1. Do not touch working features unless required.
2. Do not change Room schema: exactly 5 tables, no new columns.
3. Do not touch onboarding/calendar views/sync/holidays/tasks unless task requires it.
4. Do not run phone/manual UI QA unless user asks.
5. Build after app-code change with `.\gradlew.bat --no-daemon --console=plain :app:assembleDebug`.
6. No Hilt, no Compose Nav graph. Use existing manual DI + overlays.
7. Reuse existing UI components and `mono`; no new fonts/component variants unless needed.
